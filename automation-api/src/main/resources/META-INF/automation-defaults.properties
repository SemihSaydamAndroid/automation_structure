# automation-api defaults (all api.* keys are visible in karate-config.js via KarateBridge.config())
api.base-url=
api.threads=4
api.tags=
api.report-dir=target/karate-reports
api.connect-timeout=10000
api.read-timeout=30000
